# 백엔드(Spring Boot) 주소
# - 비워두면: npm run dev 시 요청이 /api 로 가고, Vite가 localhost:8080 으로 넘김 (CORS 거의 안 탐)
# - 직접 지정: 브라우저→백엔드 직접 호출 (CORS는 서버 CorsConfig에 프론트 출처 허용 필요)
# VITE_API_BASE_URL=http://localhost:8080
